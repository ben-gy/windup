{
  "name": "windup",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Program three moves, wind it up, and watch it all go wrong.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "icons": "node scripts/gen-icons.mjs"
  },
  "dependencies": {
    "trystero": "^0.21.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.2.6",
    "@types/node": "^22.0.0",
    "jsdom": "^25.0.0"
  }
}
